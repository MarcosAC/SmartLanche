name: Auto assign
on:
  issues:
    types: [opened]

jobs:
  assign:
    runs-on: ubuntu-latest
    steps:
      - uses: kentaro-m/auto-assign-action@v1
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"
          add-reviewers: false
