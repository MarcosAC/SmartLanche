<UserControl x:Class="SmartLanche.Views.ProductRegistrationView "
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartLanche.Views"
             xmlns:vm="clr-namespace:SmartLanche.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

        <Grid Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,8" Grid.Row="0" HorizontalAlignment="Left">
                <Button 
                    Content="Novo"
                    Width="80"
                    Command="{Binding NewProductCommand}"
                    Margin="0,0,8,0" />
                
                <Button 
                    Content="Salvar"
                    Width="80"
                    Command="{Binding SaveProductCommand}"
                    Margin="0,0,8,0" />
                
                <Button
                    Content="Excluir"
                    Width="80"
                    Command="{Binding DeleteProductCommand}" />
            </StackPanel>

            <DataGrid Grid.Row="1" ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct, Mode=TwoWay}" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn
                        Header="Id"
                        Binding="{Binding Id}"
                        IsReadOnly="True"
                        Width="60" />
                    
                    <DataGridTextColumn
                        Header="Nome"
                        Binding="{Binding Name, Mode=TwoWay,
                        UpdateSourceTrigger=PropertyChanged}"
                        Width="*" />
                    
                    <DataGridTextColumn
                        Header="Categoria"
                        Binding="{Binding Category,
                        Mode=TwoWay,
                        UpdateSourceTrigger=PropertyChanged}"
                        Width="150" />
                    
                    <DataGridTextColumn
                        Header="Preço"
                        Binding="{Binding Price,
                        Mode=TwoWay,
                        StringFormat={}{0:C}}"
                        Width="100" />
                    
                    <DataGridTextColumn
                        Header="Descrição"
                        Binding="{Binding Description,
                        Mode=TwoWay, 
                        UpdateSourceTrigger=PropertyChanged}"
                        Width="2*" />
                    
                    <DataGridCheckBoxColumn
                        Header="Combo"
                        Binding="{Binding EhCombo}"
                        Width="80" />
            </DataGrid.Columns>
            </DataGrid>
        </Grid>
</UserControl>
