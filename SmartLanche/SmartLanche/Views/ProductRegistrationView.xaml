<UserControl x:Class="SmartLanche.Views.ProductRegistratitionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartLanche.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel 
            Orientation="Horizontal"
            Grid.Row="0"
            Margin="0,0,0,8">
            
            <Button 
                Content="Novo"
                Width="80"
                Click="New_Click"
                Margin="0,0,0,8" />
            
            <Button
                Content="Salvar"
                Width="80"
                Click="Save_Click"
                Margin="0,0,0,8" />
            
            <Button
                Content="Excluir"
                Width="80"
                Click="Delete_Click"
                Margin="0,0,0,8" />
        </StackPanel>

        <DataGrid 
            x:Name="GridProducts"
            Grid.Row="1"
            AutoGenerateColumns="False"
            IsReadOnly="False">

            <DataGridTextColumn 
                Header="Id"
                Binding="{Binding Id}"
                IsReadOnly="True"
                Width="60" />

            <DataGridTextColumn 
                Header="Nomne"
                Binding="{Binding Name}"
                Width="*" />

            <DataGridTextColumn 
                Header="Categoria"
                Binding="{Binding Category}"
                Width="150" /> 
            
            <DataGridTextColumn 
                Header="Preço"
                Binding="{Binding Price}"
                Width="100" />

            <DataGridCheckBoxColumn
                Header="Combo"
                Binding="{Binding EhCombo}"
                Width="80" />
            
            <DataGridTextColumn 
                Header="Descrição"
                Binding="{Binding Description}"
                Width="200" />
        </DataGrid>            
    </Grid>
</UserControl>
