<UserControl x:Class="SmartLanche.Views.StockHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartLanche.Views"             
             xmlns:helpers="clr-namespace:SmartLanche.Helpers"             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <helpers:EnumToDisplayNameConverter x:Key="EnumToDisplayNameConverter" />        
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Histórico de Estoque" 
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0,0,0,10" />

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,15">
            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="Filtrar por Data:"
                           FontSize="12"
                           Foreground="Gray"/>
                <DatePicker SelectedDate="{Binding FilterDate, Mode=TwoWay}" Width="150"/>
            </StackPanel>

            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="Tipo:"
                           FontSize="12"
                           Foreground="Gray"/>
                <ComboBox ItemsSource="{Binding FilterTypes}"
                          SelectedItem="{Binding SelectedFilterType}"
                          DisplayMemberPath="DisplayName"
                          Width="120" />
            </StackPanel>

            <Button Content="Limpar Filtros"
                    Command="{Binding ClearFiltersCommand}" 
                    VerticalAlignment="Bottom" Padding="10,2" />
        </StackPanel>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding MovementsView}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Data"
                                    Binding="{Binding Date, StringFormat={}{0:dd/MM/yy HH:mm}}"
                                    Width="120" />
                <DataGridTextColumn Header="Produto"
                                    Binding="{Binding Product.Name}"
                                    Width="*" />

                <DataGridTextColumn Header="Qtd" 
                                    Binding="{Binding Quantity}" 
                                    ElementStyle="{StaticResource RightAlignedColumnStyle}" 
                                    Width="80" />

                <DataGridTextColumn Header="Tipo" 
                                    Binding="{Binding Type, Converter={StaticResource EnumToDisplayNameConverter}}"
                                    Width="100" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
